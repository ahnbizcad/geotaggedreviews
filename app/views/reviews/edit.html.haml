.row
  .col-xs-12
    %h1.media-heading Your Review for

    = @park.address

    = filepicker_image_tag @park.image_url, w: 500, h: 400, fit: 'clip'

    = render 'form'

    = link_to "Back to Park", park_path(@park)
